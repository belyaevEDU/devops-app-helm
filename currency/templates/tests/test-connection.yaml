apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "currency.fullname" . }}-test-connection"
  labels:
    {{- include "currency.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "currency.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
